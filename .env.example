# ===========================================
# DriftSentry - Root Environment Example
# ===========================================
# This file shows all environment variables for both backend and frontend.
# Copy to .env or use backend/.env and frontend/.env.local separately.

# ======== BACKEND ========
# Server
NODE_ENV=development
PORT=3001
HOST=0.0.0.0

# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/driftsentry?schema=public

# JWT Authentication
JWT_SECRET=your-super-secret-key-change-in-production

# CORS
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=debug

# ======== FRONTEND ========
# API URL (Next.js requires NEXT_PUBLIC_ prefix for client-side vars)
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
